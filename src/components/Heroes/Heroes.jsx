import HeroCard from "../Cards/HeroCard";
const Heroes = (props) => {
    return(
        <section className="card-container">
            <HeroCard />
        </section>
    );
}

export default Heroes;